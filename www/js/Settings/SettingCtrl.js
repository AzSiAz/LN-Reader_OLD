angular.module('ln.SettingCtrl', [])

.controller('SettingCtrl', function($scope) {
  // db.removeCache("all").then(function(res) {
  //   $cordovaDialogs.alert(JSON.stringify(res), "Nombre Cache");
  // }, function(err) {
  //   $cordovaDialogs.alert(JSON.stringify(err), "Erreur recuperation nombre cache");
  // })
});